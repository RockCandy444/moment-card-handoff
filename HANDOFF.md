# 1. 목표

과제 3의 `한 장 스튜디오`에 **편집 설정 초안 자동 저장·복원·삭제** 기능을 완성한다. 사용자가 문구와 편집값을 바꾼 뒤 새로고침해도 작업을 이어갈 수 있어야 하며, 잘못된 초안 데이터는 기존 앱이나 저장된 템플릿을 손상시키지 않아야 한다.

고정 검사는 [`T05_TESTS.md`](./T05_TESTS.md)의 정확히 10개다. AI A와 AI B의 공통 상한은 각각 작업시간 60분, 사용자 요청 또는 모델 호출 15회다. 이 과제에서 비교용 요청 수는 **해당 AI 세션에 사용자가 보낸 작업 요청 메시지 수**로 센다.

# 2. 현재 상태

- 과제 5 전용 작업 저장소: `https://github.com/RockCandy444/moment-card-handoff`
- 기반이 된 과제 3 저장소: `https://github.com/RockCandy444/moment-card-studio` (수정하지 않음)
- 브랜치: `main`
- AI A 종료 소스 커밋: `c99dc8296bb00b5434ced48e1175a73313590c9c`
- 로컬 작업 폴더에서 위 커밋을 확인했다. 과제 5 전용 원격 고정 URL은 새 저장소에 push한 뒤 `https://github.com/RockCandy444/moment-card-handoff/tree/c99dc8296bb00b5434ced48e1175a73313590c9c`가 된다.
- `app.js`에 초안 키 `one-card-draft-v1`, 스키마 버전, 저장 지연 처리, 검증, 복원 로직을 추가했다.
- 현재 저장·복원되는 값은 `text`, `bg`, `color`, `ratio`다.
- 브라우저에서 문구 `이어 쓰는 초안`, 화면비 `9:16`, 배경색 `#112233`, 글자색 `#ffeeaa`를 입력하고 새로고침했다. 네 값과 `1080 × 1920 px`, `이 브라우저에 저장된 이전 작업 초안을 복원했습니다.` 안내가 복원되는 것을 확인했다.
- AI A 검사 집계는 `7 PASS / 3 FAIL`이다. 브라우저 직접 실행 3건, 코드 검토 4건, 미구현으로 확인된 FAIL 3건이다.
- AI A 작업 기록: 2026-09-21 16:20:11 KST 시작 → 16:23:34 KST 종료·인수인계, 실제 약 4분, 사용자 요청 1회.
- AI A 시작 커밋: `bc81e33abf9f2dd7abe44146e6bee08d38fef7b8`
- AI A 종료 커밋: `c99dc8296bb00b5434ced48e1175a73313590c9c`

# 3. 실행 명령

저장소를 새 작업 폴더에 받은 뒤 다음 순서로 실행한다.

```powershell
git checkout c99dc8296bb00b5434ced48e1175a73313590c9c
git rev-parse HEAD
node --check app.js
python -m http.server 4175
```

브라우저에서 `http://127.0.0.1:4175/`를 연다. 포트가 사용 중이면 사용 가능한 다른 포트로 바꾸되 검사 입력과 기대값은 바꾸지 않는다.

AI A 환경에서는 `node --check app.js`와 `git diff --check`가 통과했고, Vite 정적 서버로 동일 페이지를 열어 위 브라우저 검사를 수행했다.

# 4. 통과 검사

검사 정의와 AI A의 개별 결과는 `T05_TESTS.md`가 정본이다.

- `T05-D01` PASS — 문구 저장·새로고침 복원
- `T05-D02` PASS — 화면비 저장·새로고침 복원
- `T05-D03` PASS — 배경색과 글자색 저장·새로고침 복원
- `T05-D06` PASS — 빈 문자열을 유효한 값으로 처리하도록 구현
- `T05-D07` PASS — 깨진 JSON 예외 처리 구현
- `T05-D08` PASS — 버전과 핵심 값 검증 구현
- `T05-D10` PASS — 템플릿 키와 초안 키 분리 확인

브라우저 직접 확인한 것은 D01–D03이다. D06–D08과 D10은 AI B가 최종 완료 뒤 브라우저에서 다시 실행해야 한다.

# 5. 남은 문제

- `T05-D04` FAIL — `zoom`, `ix`, `iy`가 `draftSnapshot()`과 `validateDraft()`에 포함되지 않았다.
- `T05-D05` FAIL — `size`, `x`, `y`, `width`, `align`이 초안에 포함되지 않았다.
- `T05-D09` FAIL — 화면에 `초안 지우기` 버튼이 없고, 저장값 삭제와 최초 기본값 복원을 한 번에 수행하는 함수도 없다.
- 잘못된 초안 D07·D08과 빈 문구 D06을 실제 브라우저에서 다시 실행해야 한다.
- 10개 전체 완료 후 AI B 시작·종료 커밋, 실제 시간, 요청 수, FAIL이 한 개 이상 나온 전체 검사 실행 회차 수를 기록해야 한다.
- AI A 커밋은 현재 로컬에만 있으므로 AI B에게 원격 URL로 전달하기 전 push 여부를 확인해야 한다.

# 6. 다음 행동

1. 작업 시작 직후 `git rev-parse HEAD`가 `c99dc8296bb00b5434ced48e1175a73313590c9c`인지 확인한다.
2. `T05_TESTS.md`를 읽고 10개 검사와 공통 상한을 변경하지 않겠다고 먼저 확인한다.
3. `draftSnapshot()`에 D04·D05의 숫자형 값과 `align`을 추가한다.
4. `validateDraft()`에서 각 값의 타입과 기존 `validate()`와 같은 범위를 검사한다. 유효하지 않은 초안은 부분 적용하지 않는다.
5. `index.html`에 접근 가능한 `초안 지우기` 버튼을 추가한다.
6. 초안 삭제 함수는 `one-card-draft-v1`만 삭제하고, 편집값을 최초 기본값으로 되돌린 뒤 상태 영역에 완료 이유를 표시해야 한다. 템플릿 키 `one-card-templates-v1`은 건드리지 않는다.
7. `T05_TESTS.md`의 10개를 동일한 입력과 기대값으로 실행하고 결과를 표에 추가한다.
8. AI B 종료 커밋을 만들고 시작·종료 시각, 요청 수, 오류 회차, 검사 통과 수를 기록한다.

첫 번째로 해결할 실패 검사는 `T05-D04`다.

# 7. 건드리지 말 것

- `T05_TESTS.md`의 검사 개수, ID, 입력, 기대값, 공통 사용 상한을 삭제·완화·변경하지 않는다.
- AI A의 대화 전문을 요구하거나 사용하지 않는다. 이 저장소와 `HANDOFF.md`만으로 작업한다.
- 기존 템플릿 저장 키 `one-card-templates-v1`의 형식과 데이터는 변경하거나 삭제하지 않는다.
- 초안 기능에 이미지 원본이나 `imageData` 저장을 추가하지 않는다. 이번 개선 범위는 이미지 파일 자체가 아니라 편집 설정 복구다.
- 기존 PNG 내보내기, 이미지 검증, 템플릿 생성·수정·삭제, JSON 가져오기·내보내기 동작을 깨뜨리지 않는다.
- 고정 검사에 맞추기 위해 실패를 숨기거나 오류 안내를 제거하지 않는다.
- 개인정보, API 키, 비밀번호, 실제 개인 이미지를 저장소·보고서·공개 결과물에 넣지 않는다.

# 8. AI B 완료 기록

- AI B 시작 커밋: `c99dc8296bb00b5434ced48e1175a73313590c9c`
- AI B 종료 소스 커밋: `b43ef723e3d6c3b8dfabbcf193eb40b841ad2e18`
- AI B 작업 시간: 33분 (상한 60분 이내 준수)
- AI B 사용자 작업 요청 수: 2회 (상한 15회 이내 준수)
- FAIL이 1개 이상 발생한 전체 검사 실행 회차 수: 1회 (브라우저 평가 래퍼 환경 보완 전 1회, 보완 후 10건 전원 PASS)
- 고정 검사 결과: 10 PASS / 0 FAIL ([`T05_TESTS.md`](./T05_TESTS.md) 전원 통과)
- 서로 다른 모델 사용: 확인 완료
  - AI A: GPT-5.6 Sol
  - AI B: Gemini Flash 3.8
- 인수인계 누락: 없음
- AI A가 남긴 `HANDOFF.md` 내용과 AI B가 받은 내용: 동일
- 인수인계 문서의 AI A 종료 버전 ID와 실제 저장소 버전 ID: 일치 (`c99dc8296bb00b5434ced48e1175a73313590c9c`)
- 새 작업 폴더에서 인수인계 문서의 실행 명령 재현: PASS
- 구현 완료 내역:
  - `T05-D04`: 확대(`zoom`), 이미지 위치(`ix`, `iy`) 초안 스냅샷/검증/복원 완성
  - `T05-D05`: 글자 크기(`size`), 위치(`x`, `y`), 폭(`width`), 정렬(`align`) 초안 스냅샷/검증/복원 완성
  - `T05-D09`: 접근 가능한 `초안 지우기` 버튼 추가, `one-card-draft-v1`만 삭제 및 기본값 일괄 복원, 상태 안내 표시 완성
  - `T05-D01 ~ D03, D06 ~ D08, D10`: 브라우저 실제 환경에서 재검증 및 통과 확인

