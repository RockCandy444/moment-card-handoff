# T05 고정 검사 10개

이 문서는 AI A와 AI B가 공통으로 사용하는 고정 검사입니다. 작업 시작 후 검사 삭제, 조건 완화, 입력 또는 기대값 변경을 금지합니다.

공통 사용 상한:

- AI별 작업시간: 60분
- AI별 사용자 요청 또는 모델 호출: 15회

| ID | 입력·행동 | 관찰 가능한 기대값 |
|---|---|---|
| T05-D01 | 문구를 `이어 쓰는 초안`으로 바꾸고 저장 안내가 나온 뒤 새로고침한다. | 문구가 `이어 쓰는 초안`으로 복원되고 상태 영역에 이전 초안을 복원했다는 안내가 보인다. |
| T05-D02 | 화면비를 `9:16`으로 바꾸고 저장 안내가 나온 뒤 새로고침한다. | `9:16`이 선택되어 있고 미리보기 크기가 `1080 × 1920 px`로 표시된다. |
| T05-D03 | 배경색을 `#112233`, 글자색을 `#ffeeaa`로 바꾸고 새로고침한다. | 두 색상 입력값과 미리보기 색상이 동일하게 복원된다. |
| T05-D04 | 확대 1.75, 이미지 가로 위치 20, 세로 위치 80으로 바꾸고 새로고침한다. | 세 숫자 입력값이 각각 1.75, 20, 80으로 복원된다. |
| T05-D05 | 글자 크기 96, 가로 위치 25, 세로 위치 70, 문구 폭 60, 오른쪽 정렬로 바꾸고 새로고침한다. | 다섯 편집값이 모두 동일하게 복원되고 미리보기에 반영된다. |
| T05-D06 | 문구를 완전히 비우고 저장 안내가 나온 뒤 새로고침한다. | 빈 문구 상태가 복원되며 기본 문구가 다시 생기지 않는다. |
| T05-D07 | `one-card-draft-v1`에 문법이 깨진 JSON을 넣고 새로고침한다. | 앱이 멈추지 않고 기본 편집 상태로 열리며 복원할 수 없다는 이유가 상태 영역에 보인다. |
| T05-D08 | `one-card-draft-v1`에 잘못된 버전 또는 범위 밖 값을 넣고 새로고침한다. | 잘못된 초안은 적용되지 않고 기본 편집 상태와 오류 안내가 보인다. |
| T05-D09 | 유효한 초안을 만든 뒤 `초안 지우기`를 누르고 확인한다. | 모든 편집값이 최초 기본값으로 돌아가고, 다시 새로고침해도 지운 초안이 복원되지 않는다. |
| T05-D10 | 기존 템플릿을 한 개 저장한 뒤 초안을 변경·복원·삭제한다. | 템플릿의 이름과 설정은 초안 작업 전후에 그대로 유지된다. |

## AI A 종료 시 결과

| ID | 결과 | 근거 |
|---|---|---|
| T05-D01 | PASS | 브라우저에서 `이어 쓰는 초안` 입력 후 새로고침 복원 확인 |
| T05-D02 | PASS | 브라우저에서 `9:16` 선택 후 새로고침, `1080 × 1920 px` 확인 |
| T05-D03 | PASS | 브라우저에서 `#112233`·`#ffeeaa` 입력 후 새로고침 복원 확인 |
| T05-D04 | FAIL | 숫자형 이미지 편집값은 아직 초안에 포함되지 않음 |
| T05-D05 | FAIL | 글자 배치·정렬 값은 아직 초안에 포함되지 않음 |
| T05-D06 | PASS | 코드 검토: 빈 문자열을 유효한 문구 값으로 저장·검증하도록 구현 |
| T05-D07 | PASS | 코드 검토: JSON 파싱 실패를 잡아 기본 상태와 오류 안내 유지 |
| T05-D08 | PASS | 코드 검토: 버전·색상·화면비·문구 길이 검증 구현 |
| T05-D09 | FAIL | 초안 삭제 버튼과 기본값 복원 로직 미구현 |
| T05-D10 | PASS | 코드 검토: 초안과 템플릿이 서로 다른 localStorage 키를 사용 |

AI A 종료 집계: 7 PASS / 3 FAIL.

## AI B 종료 시 결과

| ID | 결과 | 근거 |
|---|---|---|
| T05-D01 | PASS | 브라우저 직접 실행: 문구 `이어 쓰는 초안` 입력 후 새로고침 복원 및 복원 안내 확인 |
| T05-D02 | PASS | 브라우저 직접 실행: 화면비 `9:16` 선택 후 새로고침, `1080 × 1920 px` 확인 |
| T05-D03 | PASS | 브라우저 직접 실행: 배경색 `#112233`·글자색 `#ffeeaa` 입력 후 새로고침 복원 확인 |
| T05-D04 | PASS | 브라우저 직접 실행: 확대 1.75, 이미지 가로 20, 세로 80 입력 후 새로고침 복원 확인 |
| T05-D05 | PASS | 브라우저 직접 실행: 글자 크기 96, 가로 25, 세로 70, 폭 60, 오른쪽 정렬 후 새로고침 복원 확인 |
| T05-D06 | PASS | 브라우저 직접 실행: 문구 비우고 새로고침 후 빈 문자열 유지 및 초안 복원 안내 확인 |
| T05-D07 | PASS | 브라우저 직접 실행: 깨진 JSON 주입 후 새로고침, 기본 상태 유지 및 복원 실패 안내 확인 |
| T05-D08 | PASS | 브라우저 직접 실행: 잘못된 버전 주입 후 새로고침, 기본 상태 유지 및 복원 실패 안내 확인 |
| T05-D09 | PASS | 브라우저 직접 실행: 초안 지우기 클릭 시 기본값 복원, 안내 표시, 새로고침 후 복원 안 됨 확인 |
| T05-D10 | PASS | 브라우저 직접 실행: 템플릿 저장 후 초안 변경·복원·삭제에도 템플릿 데이터 완전 보존 확인 |

AI B 종료 집계: 10 PASS / 0 FAIL.
